#include <bits/stdc++.h>
using namespace std;

int main()
{
    // Multimap Set - Not Unique (Multiple element) and sorted order - O(logn)
    multimap<int, int> mp;
    // only mp[key] cannot be used here
    //  lower and upper bound not works
    //  Work rest of funations are same
    //  It does not store any particular order it has a better complexity than set in most cases except some when collision happend

    return 0;
}